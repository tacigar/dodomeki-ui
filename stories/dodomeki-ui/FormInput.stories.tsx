import { storiesOf } from '@storybook/react';
import React from 'react';
import { FormInput } from '../../packages/dodomeki-ui/src/FormInput';

storiesOf('FormInput', module).add('FormInput', () => <FormInput isLoading />);
